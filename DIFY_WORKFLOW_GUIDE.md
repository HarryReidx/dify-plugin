# Dify 工作流配置指南

## 问题诊断

如果您看到错误 "错误：未提供 checklist_json 参数"，说明工具没有接收到输入数据。

## 解决方案

### 方案 1: 检查参数连接

在 Dify 工作流中：

1. **确认工具节点配置**
   - 打开工具节点设置
   - 找到 `checklist_json` 参数输入框
   - 确保该字段不为空

2. **正确连接上游节点**
   ```
   [LLM 节点] → 输出变量 → [工具节点] checklist_json 参数
   ```

3. **检查变量映射**
   - 如果从 LLM 获取 JSON，确保选择了正确的输出变量
   - 变量名应该映射到 `checklist_json`

### 方案 2: 直接输入 JSON

如果不使用上游节点，可以直接在工具节点中输入 JSON：

1. 点击工具节点
2. 在 `checklist_json` 参数框中粘贴 JSON 字符串
3. 使用最小示例测试：

```json
{
  "tableName": "测试检查表",
  "columns": ["序号", "内容"],
  "sections": [
    {
      "title": "第一部分",
      "items": [
        {"id": "1", "content": "测试项"}
      ]
    }
  ]
}
```

### 方案 3: 使用变量引用

如果使用工作流变量：

1. **创建变量节点**
   - 添加"变量"节点
   - 设置变量值为 JSON 字符串

2. **连接到工具节点**
   - 将变量节点的输出连接到工具节点的 `checklist_json` 参数

### 方案 4: 从 LLM 生成 JSON

如果使用 LLM 生成 JSON：

1. **配置 LLM 提示词**
```
请生成一个安全检查表的 JSON 数据，格式如下：

{
  "tableName": "检查表名称",
  "columns": ["序号", "检查内容", "检查方法", "参考依据", "分值"],
  "sections": [
    {
      "title": "分组标题",
      "items": [
        {
          "id": "1.1",
          "content": "检查内容",
          "method": "检查方法",
          "reference": "参考依据",
          "score": "10"
        }
      ]
    }
  ]
}

请只输出 JSON，不要包含其他文字。
```

2. **连接 LLM 输出**
   - 选择 LLM 节点的 `text` 输出
   - 连接到工具节点的 `checklist_json` 参数

## 常见错误

### 错误 1: 参数为空字符串

**症状**: "错误：未提供 checklist_json 参数"

**原因**: 
- 参数框为空
- 上游节点输出为空
- 变量未正确连接

**解决**:
- 检查参数是否有值
- 查看上游节点是否正常执行
- 确认变量连接正确

### 错误 2: JSON 格式错误

**症状**: "JSON 解析错误：..."

**原因**:
- JSON 格式不正确
- 包含多余的文字
- 引号使用错误

**解决**:
- 使用 JSON 验证工具检查格式
- 确保 LLM 只输出 JSON
- 使用双引号，不要用单引号

### 错误 3: columns 为空

**症状**: "错误：columns 字段为空"

**原因**:
- JSON 中 `columns` 数组为空
- `columns` 字段缺失

**解决**:
- 确保 JSON 包含 `columns` 字段
- `columns` 至少包含一个元素

## 调试步骤

### 步骤 1: 查看详细错误信息

更新后的工具会显示：
- 接收到的参数键
- 参数值
- 详细的错误提示

### 步骤 2: 使用最小示例

先用最简单的 JSON 测试：

```json
{"tableName":"测试","columns":["序号"],"sections":[{"title":"测试","items":[{"id":"1"}]}]}
```

### 步骤 3: 逐步增加复杂度

测试通过后，再使用完整的 JSON：

```json
{
  "tableName": "施工现场安全检查表",
  "columns": ["序号", "检查内容", "检查方法", "参考依据", "分值"],
  "sections": [
    {
      "title": "一、安全管理",
      "items": [
        {
          "id": "1.1",
          "content": "施工单位应建立健全安全生产责任制度",
          "method": "查阅安全管理制度文件",
          "reference": "《建设工程安全生产管理条例》第21条",
          "score": "10"
        }
      ]
    }
  ]
}
```

## 工作流示例

### 示例 1: 直接输入

```
[开始] → [工具节点: HSE安全检查表生成器]
         └─ checklist_json: {直接粘贴 JSON}
```

### 示例 2: 从 LLM 生成

```
[开始] → [LLM 节点] → [工具节点: HSE安全检查表生成器]
         └─ 提示词      └─ checklist_json: {{LLM.text}}
```

### 示例 3: 使用变量

```
[开始] → [变量节点] → [工具节点: HSE安全检查表生成器]
         └─ JSON 数据   └─ checklist_json: {{variable.value}}
```

## 验证清单

在运行工作流前，请确认：

- [ ] 工具节点已添加到工作流
- [ ] `checklist_json` 参数已配置
- [ ] 参数值不为空
- [ ] JSON 格式正确（可用在线工具验证）
- [ ] 包含必需字段：tableName, columns, sections
- [ ] columns 数组不为空
- [ ] 上游节点（如有）正常执行

## 测试建议

1. **先本地测试**
   ```bash
   python test_tool.py
   ```
   确保代码逻辑正确

2. **使用最小 JSON**
   先用简单的 JSON 测试连接是否正常

3. **查看执行日志**
   在 Dify 中查看工作流执行日志，了解详细错误

4. **逐步调试**
   从简单到复杂，逐步排查问题

## 获取帮助

如果问题仍未解决：

1. 查看更新后的错误信息（包含参数详情）
2. 检查 Dify 版本是否 >= 0.4.0
3. 确认插件已正确安装和启用
4. 查看 Dify 后台日志

## 成功标志

当配置正确时，您应该看到：
- 工作流执行成功
- 生成 Excel 文件
- 可以下载文件
- 文件格式正确

---

**提示**: 如果您看到详细的调试信息（参数键和值），请将这些信息用于诊断问题。
